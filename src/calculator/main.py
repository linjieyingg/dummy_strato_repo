"""
This module serves as the main entry point for the calculator application's
command-line interface (CLI).

It imports and executes the main function defined in src.calculator.cli,
which handles argument parsing and calculation.
"""

from src.calculator.cli import main

if __name__ == "__main__":
    main()