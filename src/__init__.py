"""
Declares the 'src' directory as a Python package.

This enables Python to recognize directories within 'src' as subpackages
and allows for relative imports within the 'src' package structure.
"""
# No other content is necessary for the stated purpose of declaring
# 'src' as a package and enabling relative imports.
# Further functionality (e.g., exposing submodules, defining __version__)
# would be added here if required by specific design decisions,
# but it is not requested by the current purpose.