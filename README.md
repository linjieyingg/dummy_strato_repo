markdown
# Dice Rolling App

This application provides a command-line interface for simulating dice rolls. You can specify the number of dice, the number of sides per die, and optionally request the sum of the rolls.

## How to Run

To run the dice rolling app, use the following command structure: